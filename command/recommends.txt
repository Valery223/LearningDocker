
1) How instaal Docker & Docker compose:
1] curl -fsSl https://get.docker.com -o get-docker.sh # -o куда установить
2] sh ./get-docker.sh

2) application - portainer.io:

how install portainer.io:
1] on site in information on install(or check video)
